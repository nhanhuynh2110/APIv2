const {countDocument, excuteQuery, findOneId, save, update} = require('../../model/mongo/util')
const {Product} = require('../../model/mongo')

module.exports = {
  findId: (req, res, id) => {

  }
}
